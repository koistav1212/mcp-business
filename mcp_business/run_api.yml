info:
  name: run_api
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/workspace/run
  body:
    type: json
    data: '{"query": "Analyze Apple"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​